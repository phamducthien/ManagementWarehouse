//------------------------------------------------------------------------------
// <auto-generated>
// Service Generated By Nick Nguyen
// </auto-generated>
//------------------------------------------------------------------------------
using System;
using System.Collections.Generic;
using System.Linq;
using System.Linq.Expressions;
using Repository.Pattern.UnitOfWork;
using Service.Pattern;
using WH.Entity;
using WH.Model;
using WH.Service.Repository;
using System.Data.Entity.Validation;
using System.Data.Entity.Infrastructure;
using WH.Service.Service;
namespace WH.Service.Service
{
    public partial interface IPHIEUCHIService : IService
    {
    	bool Exist(string id);
    	bool Exist(Expression<Func<PHIEUCHI, bool>> predicate);
    
    	PHIEUCHI CreateNew();
        List<PHIEUCHI> FindAll();
    	List<PHIEUCHI> Search(string textSearch);
    	List<PHIEUCHI> Search(Expression<Func<PHIEUCHI, bool>> predicate,string textSearch);
        List<PHIEUCHI> Search(Expression<Func<PHIEUCHI, bool>> predicate);
    	
    	PHIEUCHI Find(string idUnit);
    	PHIEUCHI Find(Expression<Func<PHIEUCHI, bool>> predicate);
    
    	//PHIEUCHI CloneNew(PHIEUCHI objPHIEUCHI);
    	//PHIEUCHI CloneUpdate(PHIEUCHI objPHIEUCHI);
    	PHIEUCHI Clone(PHIEUCHI objPHIEUCHI);
    	List< PHIEUCHI> Clone(IList<PHIEUCHI> listPHIEUCHIs);
    
        MethodResult Add(PHIEUCHI model, bool isCommited = false,bool isAddChild = false);
        MethodResult Modify(PHIEUCHI model, bool isCommited= false,bool isModifyChild = false);
    	MethodResult Remove(PHIEUCHI model, bool isCommited = false,bool isRemoveChild = false);
        MethodResult Remove(string idUnit, bool isCommited = false,bool isRemoveChild = false);
    }
    
    public partial class PHIEUCHIService : global::Service.Pattern.Service, IPHIEUCHIService
    {
    	protected string userId = SessionModel.CurrentSession?.UserId;
    	protected IPHIEUCHIRepository _PHIEUCHIRepository;
    	
    
    	public PHIEUCHIService(IUnitOfWorkAsync unitOfWork)
                : base(unitOfWork)
        {
        }
    
        protected override void InitRepositories()
        {
            _PHIEUCHIRepository = new PHIEUCHIRepository(this._dataContext, this._unitOfWork);
    		
        }
    	
    	public bool Exist(string id)
        {
    		return _PHIEUCHIRepository.Exist(id);
        }
    
    	public bool Exist(Expression<Func<PHIEUCHI, bool>> predicate)
    	{
    		return _PHIEUCHIRepository.Exist(predicate);
    	}
    
        public PHIEUCHI CreateNew()
        {
            return _PHIEUCHIRepository.CreateNew();
        }
    
    	public List<PHIEUCHI> FindAll()
    	{
    		return _PHIEUCHIRepository.FindAll();
    	}
    
    	public List<PHIEUCHI> Search(string textSearch)
        {
            return _PHIEUCHIRepository.Search(textSearch);
        }
    
    	public List<PHIEUCHI> Search(Expression<Func<PHIEUCHI, bool>> predicate,string textSearch)
    	{
    		  return _PHIEUCHIRepository.Search(predicate, textSearch);
    	}
    
    		public PHIEUCHI Find(Expression<Func<PHIEUCHI, bool>> predicate)
            {
               return _PHIEUCHIRepository.getObject(predicate);
            }
    
            public List<PHIEUCHI> Search(Expression<Func<PHIEUCHI, bool>> predicate)
            {
                return _PHIEUCHIRepository.Search(predicate);
            }
    
            //public PHIEUCHI CloneNew(PHIEUCHI objPHIEUCHI)
           // {
           //     return _PHIEUCHIRepository.CloneNew(objPHIEUCHI);
           // }
    
    	//	public PHIEUCHI CloneUpdate(PHIEUCHI objPHIEUCHI)
    	//	{
    	//		  return _PHIEUCHIRepository.CloneUpdate(objPHIEUCHI);
    	//	}
    
    		public PHIEUCHI Clone(PHIEUCHI objPHIEUCHI)
    		{
    			return _PHIEUCHIRepository.Clone(objPHIEUCHI);
    		}
    
            public List<PHIEUCHI> Clone(IList<PHIEUCHI> listPHIEUCHIs)
            {
                return _PHIEUCHIRepository.Clone(listPHIEUCHIs);
            }
    
        public PHIEUCHI Find(string idUnit)
        {
    		return _PHIEUCHIRepository.getObject(idUnit);
        }
    
    	public MethodResult Add(PHIEUCHI entity, bool isCommited,bool isAddChild)
        {
    		MethodResult result = MethodResult.Succeed;
    		try
    		{
    			if(isCommited)
    				_unitOfWork.BeginTransaction();
    
    			entity.Validate();
    
    			_PHIEUCHIRepository.Insert(Clone(entity));
    			if (SaveChanges() <= 0)
    			{
    				ThrowException("Không thể thêm dữ liệu 'PHIEUCHI'.");
    			}
    
    			if(isAddChild)
    			{
    			
    
    				if (SaveChanges() <= 0)
    				{
    					ThrowException("Không thể thêm dữ liệu con 'PHIEUCHI'.");
    				}
    			}
    
    			if(isCommited)
    					_unitOfWork.Commit();
            }
    		catch (DbEntityValidationException exception)
    		{
        	        var ex = HandleDbEntityValidationException(exception);
    
        	        if (_unitOfWork != null) _unitOfWork.Rollback();
        	        ErrMsg = ex.Message;
        	        result = MethodResult.Failed;
    		 }
    		catch (DbUpdateException exception)
    		{
                    var ex = HandleDbUpdateException(exception);
    
                    if (_unitOfWork != null) _unitOfWork.Rollback();
                    ErrMsg = ex.Message;
                    result = MethodResult.Failed;
    		}
            return result;
        }
    
    	public MethodResult Modify(PHIEUCHI entity, bool isCommited,bool isModifyChild)
    	{
                MethodResult result = MethodResult.Succeed;
                try
                {
    
                 if(isCommited)
    				_unitOfWork.BeginTransaction();
    
                    entity.Validate();
    
                    _PHIEUCHIRepository.Update(Clone(entity));
    				if (SaveChanges() <= 0)
                    {
                        ThrowException("Không thể sửa dữ liệu 'PHIEUCHI'.");
                    }
    
    				if(isModifyChild)
    				{
    				
    
    					 if (SaveChanges() <= 0)
    					{
    						ThrowException("Không thể sửa dữ liệu con 'PHIEUCHI'.");
    					}
    				}
    
                   	if(isCommited)
    					_unitOfWork.Commit();
                }
                catch (DbEntityValidationException exception)
        	    {
        	        var ex = HandleDbEntityValidationException(exception);
    
        	        if (_unitOfWork != null) _unitOfWork.Rollback();
        	        ErrMsg = ex.Message;
        	        result = MethodResult.Failed;
        	    }
        	    catch (DbUpdateException exception)
        	    {
                    var ex = HandleDbUpdateException(exception);
    
                    if (_unitOfWork != null) _unitOfWork.Rollback();
                    ErrMsg = ex.Message;
                    result = MethodResult.Failed;
                }
                return result;
            }
    
            public MethodResult Remove(string idUnit, bool isCommited,bool isRemoveChild)
            {
                MethodResult result = MethodResult.Succeed;
    
                try
                {
    				if(isCommited)
    						_unitOfWork.BeginTransaction();
    
    
                    PHIEUCHI entity = Find(idUnit);
    
                    if (entity == null)
                    {
                        ThrowException("Dữ liệu 'PHIEUCHI' này không tồn tại trong hệ thống.");
                    }
    				if(isRemoveChild)
    				{
    				
    
    				    if (SaveChanges() <= 0)
    					{
    						ThrowException("Không thể xóa dữ liệu con 'PHIEUCHI'.");
    					}
    				}
    
                    _PHIEUCHIRepository.Delete(Clone(entity));
    
                    if (SaveChanges() <= 0)
                    {
                        ThrowException("Không thể xóa dữ liệu 'PHIEUCHI'.");
                    }
    
    				if(isCommited)
    						_unitOfWork.Commit();
                }
                catch (DbEntityValidationException exception)
        	    {
        	        var ex = HandleDbEntityValidationException(exception);
    
        	        if (_unitOfWork != null) _unitOfWork.Rollback();
        	        ErrMsg = ex.Message;
        	        result = MethodResult.Failed;
        	    }
        	    catch (DbUpdateException exception)
        	    {
                    var ex = HandleDbUpdateException(exception);
    
                    if (_unitOfWork != null) _unitOfWork.Rollback();
                    ErrMsg = ex.Message;
                    result = MethodResult.Failed;
                }
                return result;
            }
    
    		public MethodResult Remove(PHIEUCHI model, bool isCommited,bool isRemoveChild)
            {
                MethodResult result = MethodResult.Succeed;
    
                try
                {
                    if(isCommited)
    						_unitOfWork.BeginTransaction();
    
    
                    PHIEUCHI entity = Find(model.IDUnit);
    
                    if (entity == null)
                    {
                        ThrowException("Dữ liệu 'PHIEUCHI' này không tồn tại trong hệ thống.");
                    }
    
    				if(isRemoveChild)
    				{
    				
    
    					if (SaveChanges() <= 0)
    					{
    						ThrowException("Không thể xóa dữ liệu con 'PHIEUCHI'.");
    					}
    				}
                    _PHIEUCHIRepository.Delete(Clone(entity));
    
                    if (SaveChanges() <= 0)
                    {
                        ThrowException("Không thể xóa dữ liệu 'PHIEUCHI'.");
                    }
    
                   	if(isCommited)
    					_unitOfWork.Commit();
                }
                catch (DbEntityValidationException exception)
        	    {
        	        var ex = HandleDbEntityValidationException(exception);
    
        	        if (_unitOfWork != null) _unitOfWork.Rollback();
        	        ErrMsg = ex.Message;
        	        result = MethodResult.Failed;
        	    }
        	    catch (DbUpdateException exception)
        	    {
                    var ex = HandleDbUpdateException(exception);
    
                    if (_unitOfWork != null) _unitOfWork.Rollback();
                    ErrMsg = ex.Message;
                    result = MethodResult.Failed;
                }
                return result;
            }
    }
}
